#ifndef _QSORT_EXAM_INTEGER_H
#define _QSORT_EXAM_INTEGER_H

#include <stdio.h>

#define SWAP(a,b) temp=(a);(a)=(b);(b)=temp;
#define M 7


int sort(int* base, int size, int* b, int s);
#endif
