#ifndef _PRIME_H
#define _PRIME_H

#include <stdio.h>
//#include "ap_cint.h"

typedef  unsigned int  bool1;
typedef  unsigned int   uint;

//bool1 divides (uint n, uint m);
//bool1 even (uint n);
//bool1 prime (uint n);

#endif
